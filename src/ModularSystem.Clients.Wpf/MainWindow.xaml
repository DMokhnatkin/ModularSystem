<Window x:Class="ModularSystem.Clients.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ModularSystem.Clients.Wpf"
        xmlns:viewModels="clr-namespace:ModularSystem.Clients.Wpf.ViewModels"
        xmlns:routedCommands="clr-namespace:ModularSystem.Clients.Wpf.ViewModels.RoutedCommands"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.DataContext>
        <viewModels:MainWindowViewModel/>
    </Window.DataContext>
    <Window.CommandBindings>
        <CommandBinding Command="{x:Static routedCommands:RoutedCommands.DownloadCommand}" Executed="CommandBinding_OnExecuted"></CommandBinding>
    </Window.CommandBindings>
    <Grid>
        <Button Content="Download" Command="{x:Static routedCommands:RoutedCommands.DownloadCommand}"></Button>
    </Grid>
</Window>
